meta {
  name: login-user
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: none
}

headers {
  ~: 
}

body:json {
  {
    "email": "admin@stagethree.com",
    "password": "admin123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
